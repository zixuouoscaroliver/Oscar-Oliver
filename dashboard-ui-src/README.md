
  # News Pusher Dashboard UI

  This is a code bundle for News Pusher Dashboard UI. The original project is available at https://www.figma.com/design/1nBfqUDfa2eIyvvm3XPrvz/News-Pusher-Dashboard-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  